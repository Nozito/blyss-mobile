/** Padding below scrollable content to clear the native tab bar + safe area */
export const TAB_BOTTOM_PADDING = 96;

/** Minimum touch target size (Apple HIG: 44pt) */
export const MIN_TOUCH_SIZE = 44;

// ─── Standard element heights — use these instead of ad-hoc values ───────────
// so buttons/inputs line up consistently across every screen.
/** Small icon-only button (back arrows, header actions, month-nav chevrons) */
export const ICON_BUTTON_SIZE = 40;
/** Larger icon tile inside list rows / cards */
export const ICON_TILE_SIZE = 44;
/** Secondary button / list-row touch height */
export const BUTTON_HEIGHT_SM = 44;
/** Standard input / secondary CTA height */
export const BUTTON_HEIGHT_MD = 48;
/** Primary CTA height (main submit/pay/confirm actions) */
export const BUTTON_HEIGHT_LG = 56;
